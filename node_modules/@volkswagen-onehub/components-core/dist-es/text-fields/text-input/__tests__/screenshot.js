import * as React from 'react';
import { ReactScreenshotTest } from 'react-screenshot-test';
import { DemoContainer, DemoRoot } from '../../../demo';
import { ThemeProvider } from '../../../theme-provider';
import { TextInput } from '../index';
import { TextInputAppearance } from '../../shared';
ReactScreenshotTest.create('Text input')
    .viewport('Desktop', {
    width: 1024,
    height: 768
})
    .shoot('Main: default style', React.createElement(DemoRoot, null,
    React.createElement(ThemeProvider, null,
        React.createElement(DemoContainer, null,
            React.createElement(TextInput, { appearance: TextInputAppearance.Default, label: "Firstname", name: "firstname" })))))
    .run();
