import * as React from 'react';
import { DemoRoot } from '../demo-root';
import { DemoContainer } from '../demo-container';
import { ThemeProvider } from '../../theme-provider';
import { DemoThemeProviderToggleContext } from '../demo-theme-provider-with-toggle';
export const DemoWrapper = props => {
    const { direction = 'ltr', theme = 'main', showGrid, language = '' } = props;
    return (React.createElement(DemoRoot, { hideMargin: true, showGrid: showGrid },
        React.createElement(ThemeProvider, { theme: theme },
            React.createElement(DemoThemeProviderToggleContext.Provider, { value: {
                    hasImageTheme: theme === 'image',
                    direction: direction,
                    language
                } },
                React.createElement(DemoContainer, { direction: direction, language: language, backgroundImage: theme === 'image'
                        ? './public/component-files/demo/blurred.png'
                        : undefined, hasBackgroundImage: theme === 'image', withoutSpacings: true, key: `demo-container-${direction}` }, props.children)))));
};
