import * as React from 'react';
import { Breakpoints } from '../helpers';
export interface BreakpointWrapperProps {
    readonly min?: Breakpoints;
    readonly max?: Breakpoints;
    readonly ssrBreakpoint?: Breakpoints;
}
export interface BreakpointWrapperState {
    readonly isVisible?: boolean;
}
export declare class BreakpointWrapper extends React.Component<BreakpointWrapperProps, BreakpointWrapperState> {
    private mediaQueryList?;
    private readonly mediaQuery;
    constructor(props: BreakpointWrapperProps);
    private readonly handleMatchMedia;
    componentDidMount(): void;
    componentWillUnmount(): void;
    render(): JSX.Element | null;
}
