import * as React from 'react';
import { DemoContainer, DemoRoot } from '../demo';
import { ThemeProvider } from '../theme-provider';
import { Spinner } from '.';
export default function SpinnerDemo() {
    return (React.createElement(DemoRoot, null,
        React.createElement("h2", null, "Spinner"),
        React.createElement(ThemeProvider, null,
            React.createElement(DemoContainer, { headline: "default" },
                React.createElement(Spinner, { variant: "default" })),
            React.createElement(DemoContainer, { headline: "large" },
                React.createElement(Spinner, { variant: "large" })),
            React.createElement(DemoContainer, { headline: "x-large" },
                React.createElement(Spinner, { variant: "x-large" })))));
}
