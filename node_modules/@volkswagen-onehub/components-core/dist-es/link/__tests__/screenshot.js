import * as React from 'react';
import { ReactScreenshotTest } from 'react-screenshot-test';
import { ThemeProvider } from '../../theme-provider';
import { DemoRoot } from '../../demo/demo-root';
import { Button } from '../../button';
ReactScreenshotTest.create('Link')
    .viewport('Desktop', {
    width: 1024,
    height: 768
})
    .shoot('empty link', React.createElement(DemoRoot, null,
    React.createElement(ThemeProvider, null,
        React.createElement(Button, null, "hallo"))))
    .run();
