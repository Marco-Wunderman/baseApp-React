import * as React from 'react';
interface AlertLayerDemoState {
    readonly withOverflowingContent: boolean;
    readonly layerIsVisible: boolean;
}
declare class AlertLayerDemo extends React.Component<void, AlertLayerDemoState> {
    readonly state: AlertLayerDemoState;
    private readonly toggleVisibility;
    private readonly toggleVisibilityEsc;
    private readonly toggleContent;
    private readonly renderOkButton;
    private readonly renderCloseButton;
    private renderCopy;
    private renderLayerContent;
    private readonly renderLayer;
    render(): JSX.Element;
}
export default AlertLayerDemo;
