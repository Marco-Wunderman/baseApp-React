import * as react from 'react';
import * as renderer from 'react-test-renderer';
import { styled } from '../../theme-provider';
export function createTestStyledComponent(css) {
    const styler = styled.span.withConfig({ displayName: "styler", componentId: "sc-113toq0" }) `
		${css}
	`;
    return renderer.create(react.createElement(styler)).toJSON();
}
