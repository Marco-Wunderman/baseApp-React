import * as React from 'react';
import { FocusLayerSize } from './index';
export interface FocusLayerDemoState {
    readonly withOverflowingContent: boolean;
    readonly layerIsVisible: boolean;
    readonly layerSize: FocusLayerSize;
}
declare class FocusLayerDemo extends React.Component<void, FocusLayerDemoState> {
    readonly state: FocusLayerDemoState;
    private readonly toggleVisibility;
    private readonly toggleContent;
    private readonly toggleSize;
    render(): JSX.Element;
}
export default FocusLayerDemo;
