import * as React from 'react';
import { styled } from '../theme-provider';
const StyledDivider = styled.hr.withConfig({ displayName: "StyledDivider", componentId: "sc-1g6khxq" }) `
	border: none;
	width: ${props => (props.isVertical ? '2px' : 'initial')};
	height: ${props => (props.isVertical ? '100%' : '2px')};
	margin: 0;
	background-color: ${props => props.theme.separatorColor};
`;
export const Divider = ({ isVertical }) => React.createElement(StyledDivider, { isVertical: isVertical });
Divider.displayName = 'Divider';
