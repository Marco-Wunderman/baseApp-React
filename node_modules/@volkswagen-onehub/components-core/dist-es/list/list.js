import * as React from 'react';
import { DesignTokenSizeEnum, styled } from '../theme-provider';
export { DesignTokenSizeEnum as ListGutter };
const StyledUList = styled.ul.withConfig({ displayName: "StyledUList", componentId: "sc-1vgh97e" }) `
	list-style-type: none;
	padding: 0;
`;
const StyledOList = styled.ol.withConfig({ displayName: "StyledOList", componentId: "sc-vag0hz" }) `
	list-style-type: none;
	padding: 0;
`;
export const List = props => {
    const { children, gutter, isOrdered = false } = props;
    const childrenArr = React.Children.toArray(children);
    const markerSize = Math.floor(Math.pow(childrenArr.length, 1 / 10)) + 1;
    const childrenElements = childrenArr.map((child, index) => React.cloneElement(child, {
        isOrdered,
        index: index + 1,
        gutter,
        markerSize
    }));
    return isOrdered ? (React.createElement(StyledOList, null, childrenElements)) : (React.createElement(StyledUList, null, childrenElements));
};
