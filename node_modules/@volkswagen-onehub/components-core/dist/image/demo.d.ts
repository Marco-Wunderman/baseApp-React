import * as React from 'react';
declare global {
    interface Window {
        readonly lazySizes?: object;
    }
}
declare class ImageDemo extends React.Component {
    render(): JSX.Element;
}
export default ImageDemo;
