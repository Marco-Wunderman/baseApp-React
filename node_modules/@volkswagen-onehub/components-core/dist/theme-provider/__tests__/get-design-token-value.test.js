"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const get_design_token_value_1 = require("../get-design-token-value");
const exampleTokens = {
    color: {
        'green-100': '#ADE5A1'
    },
    size: {
        /* eslint-disable-next-line @typescript-eslint/camelcase */
        dynamic0020: [
            { breakpoint: '0', value: '0' },
            { breakpoint: '100px', value: '10%' },
            { breakpoint: '200px', value: '20%' },
            { breakpoint: '300px', value: '30%' }
        ]
    }
};
describe('getDesignTokenValue', () => {
    test('undefined', () => {
        expect(
        /* eslint-disable-next-line @typescript-eslint/no-explicit-any */
        get_design_token_value_1.getDesignTokenValue(undefined, {
            tokens: exampleTokens
        })).toBeFalsy();
    });
    test('unknown value', () => {
        expect(get_design_token_value_1.getDesignTokenValue('wurst', { tokens: exampleTokens })).toBeFalsy();
    });
    test('basic value', () => {
        expect(get_design_token_value_1.getDesignTokenValue('color.green-100', { tokens: exampleTokens })).toBe('#ADE5A1');
    });
    test('dynamic value', () => {
        expect(get_design_token_value_1.getDesignTokenValue('size.dynamic0020', { tokens: exampleTokens })).toBe('var(--size-dynamic0020)');
    });
});
