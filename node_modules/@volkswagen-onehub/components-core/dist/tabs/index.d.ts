import * as React from 'react';
interface TabsProps {
    readonly defaultIndex: number;
    readonly onChange: (index: number) => void;
    readonly children: {
        title: React.ReactElement | string;
        content: React.ReactElement;
        key: string;
    }[];
    readonly idPrefix: string;
}
interface TabsState {
    readonly indicatorLeft: number;
    readonly indicatorWidth: number;
    readonly activeIndex: number;
}
export declare class Tabs extends React.Component<TabsProps, TabsState> {
    static defaultProps: {
        defaultIndex: number;
    };
    constructor(props: TabsProps);
    private tabsRef;
    private tabRefs;
    private updateIndicatorBoundaries;
    private updateFocus;
    componentDidMount(): void;
    componentDidUpdate(prevProps: TabsProps, prevState: TabsState): void;
    private handleClick;
    private handleKeyUp;
    render(): JSX.Element;
}
export {};
