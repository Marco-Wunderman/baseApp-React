import * as React from 'react';
export declare const _fontFamilyStyles: import("styled-components").FlattenSimpleInterpolation;
export declare const StyledIntegratorRoot: import("styled-components").StyledComponent<"div", import("../theme-provider").ThemeDefinition, {}, never>;
/**
 * On mobile browsers address bar isn't taken into account when using `vh` units. This isn't always desired so this method provides computed value that you can use to calculate proper height.
 *
 * For desktop browsers address bar height is 0px.
 *
 * Example:
 * ```
 * main {
 *   height: 100vh; // falback
 *   height: calc(100vh - ${getBrowserBarHeightValue()});
 * }
 * ```
 */
export declare function getBrowserBarHeightValue(): string;
export interface IntegratorRootProps {
    readonly language?: string;
}
export declare class IntegratorRoot extends React.Component<IntegratorRootProps> {
    private vhCheckResult?;
    private timeout?;
    private readonly updateVh;
    private readonly tryUpdateVh;
    componentDidMount(): void;
    componentDidUpdate(): void;
    componentWillUnmount(): void;
    render(): React.ReactElement;
}
