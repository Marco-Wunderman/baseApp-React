import * as React from 'react';
export interface MovingFrameProps {
    readonly claim?: React.ReactNode;
    readonly logo?: React.ReactNode;
}
export declare const MovingFrame: (props: MovingFrameProps) => JSX.Element;
